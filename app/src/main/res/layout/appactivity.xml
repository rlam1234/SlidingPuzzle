<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@color/white"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/splashView"
        android:contentDescription="@string/splash_page"
        android:layout_weight="1"
        android:visibility="visible"
        android:src="@drawable/splash_page"
        android:scaleType="fitCenter"
        android:layout_width="match_parent"
        android:layout_height="0dp" />
    <TextView
        android:id="@+id/attributionText"
        android:text="@string/attributionText"
        android:textSize="@dimen/attributionSize"
        android:padding="@dimen/titlePadding"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</LinearLayout>
